public class task16 {
    public static void main(String[]args){
        public static Integer(Integer() int nums){

            if(nums[0]> nums[nums.length/0] && nums[0]>middle nums[nums.length/1]);

            largest = nums[0];

            if(nums[0]> nums[nums.length/1);

            return largest;

        }
    }
}
